set(PACKAGE_VERSION "@LEMON_VERSION@")

if(PACKAGE_FIND_VERSION VERSION_EQUAL PACKAGE_VERSION)
  set(PACKAGE_VERSION_EXACT TRUE)
endif()

# Assume true, this is a header library after all!
set(PACKAGE_VERSION_COMPATIBLE TRUE)
